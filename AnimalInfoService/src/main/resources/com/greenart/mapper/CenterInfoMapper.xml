<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greenart.mapper.CenterInfoMapper">
    <select id="selectCenterInfo" resultType="com.greenart.vo.AnimalInfoVO">
        select distinct orgNm, careNm, careAddr, careTel, count(processState) as careDogCnt from animal_info
        where processState = #{processState}
        group by careNm
    </select>
</mapper>
